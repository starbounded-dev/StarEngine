# Binaries
bin-int/
Intermediates/
/bin

# Generated Makefiles (Linux/gmake2)
Makefile
*.make
Core/Makefile
Editor/Makefile
Lux-Runtime/Makefile
ScriptCore/Makefile

# Vendored Vulkan SDK (local install)
Core/vendor/VulkanSDK/

# Vercidium Audio (VA) raytraced audio SDK - manually fetched, requires accepting Vercidium's EULA
Core/vendor/VA_RAY/

# FMOD Engine SDK - manually fetched, requires accepting Firelight Technologies' EULA
Core/vendor/FMOD/

# JoltPhysics build artifacts
Core/vendor/JoltPhysics/Makefile
Core/vendor/JoltPhysics/bin/

# TypeScript (local)
typescript

# Premake binary (platform-specific)
premake5

# Claude Code / shared agent guidance.
# Tracked: .claude/docs (Conventions, Building, Threading, Rendering, Architecture) and
# .claude/skills (dev, cr, send-pr) - these are the single source of truth for agent workflows.
# Ignored: local settings, agent worktrees, scratch backups.
# NOTE: this must be ".claude/*", not ".claude/". Git will not descend into an excluded
# directory, so a trailing-slash exclude makes the negations below unreachable.
.claude/*
!.claude/docs/
!.claude/skills/

# Lux files
*.log

# Visual Studio files and folder
.vs/
**.sln
**.vcxproj
**.vcxproj.filters
**.vcxproj.user
**.csproj
**.csproj.user

# Directories
scripts/__pycache__

!vendor/bin/

Editor/assets/cache
Editor/Resources/Scripts/**
Sandbox/Resources/Cache
Core/vendor/NFD-Extended
Core/vendor/tracy
Editor/LuxSampleProject/Assets/Scripts/Binaries
ScriptCore/obj/
Editor/LuxSampleProject/Assets/Scripts/obj/
# Exported standalone game builds (produced by the editor's Export Runtime)
Editor/LuxSampleProject/*-Linux-x86_64/
Editor/LuxSampleProject/*-Windows-x86_64/
Editor/DotNet/
Editor/Resources/Cache
Lux.slnx
Editor/LuxSampleProject/Assets/Cache/Thumbnails
*.lap
*.aps
*.slnx
*.lib
*.pdb
Core/vendor/JoltPhysics/bin/Debug-windows-x86_64/JoltPhysics/JoltPhysics.idb
*.lsettings
Editor/dbg_trace.txt
Editor/dbg_ctor.txt

!Core/vendor/discord_social_sdk/**

# Tracy CSV-export tool build (perf tooling)
.tracytools/

# Remembered Setup/Win-GenProjects choices (per-machine)
scripts/.luxsetup.json

# JetBrains IDEs (CLion/Rider) — ignore all of .idea except the shareable
# project config. Whitelist-only, so any future per-user or IDE-generated
# file lands ignored by default. (JetBrains also drops its own
# .idea/.gitignore for the noisiest per-user files.)
.idea/*
!.idea/.gitignore
!.idea/vcs.xml
!.idea/codeStyles/
!.idea/inspectionProfiles/
!.idea/runConfigurations/

# FMOD Studio per-user workspace state (the .fspro project itself IS tracked)
Editor/LuxSampleProject/Assets/Audio/**/.user/
